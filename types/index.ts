export type instantPopUpType = {
  stop: boolean;
  date: string;
};

export type ArticleType = {
  id: number;
  title: string;
  description: string;
  link: string;
  thumbnail: string;
  published: string;
  guid: string;
  companyName: string;
  createdAt: string;
};
