export type instantPopUpType = {
  stop: boolean;
  date: string;
};

export type ArticleType = {
  thumbnail: string;
  title: string;
  description: string;
  company: string;
  author: string;
  createdAt: string;
};
